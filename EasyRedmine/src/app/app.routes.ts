import { Routes } from '@angular/router';
import { ImputationRow } from './features/imputaciones/components/imputation-row/imputation-row';

export const routes: Routes = [
    {
        path: 'imputaciones',
        component: ImputationRow
    }
];